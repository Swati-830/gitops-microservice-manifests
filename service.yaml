apiVersion: v1
kind: Service
metadata:
  name: gitops-microservice-service
  namespace: swati
spec:
  type: NodePort
  selector:
    app: gitops-microservice
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30020
